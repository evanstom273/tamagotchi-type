import type { PetState } from '../types/pet';
import { getMood, moodCopy } from '../game/personality';
function Stat({ label, value, tone }: { label: string; value: number; tone: string }) { return <div className="stat"><div className="stat__top"><span>{label}</span><strong>{value}</strong></div><div className="stat__track"><div className={`stat__fill stat__fill--${tone}`} style={{ width: `${value}%` }}/></div></div> }
export function StatusBar({ pet }: { pet: PetState }) { const mood = getMood(pet); return <aside className="status"><div className="mood"><span className={`mood__dot mood__dot--${mood}`}/><div><span className="eyebrow">Current mood</span><strong>{moodCopy[mood].label}</strong></div><span className="age">Day {Math.max(1, Math.floor(pet.age) + 1)}</span></div><div className="stats"><Stat label="Fullness" value={pet.hunger} tone="peach"/><Stat label="Joy" value={pet.happiness} tone="yellow"/><Stat label="Energy" value={pet.energy} tone="green"/><Stat label="Clean" value={pet.hygiene} tone="blue"/></div></aside> }
