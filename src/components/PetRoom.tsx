import type { PetState } from '../types/pet';
import { getMood, moodCopy } from '../game/personality';
export function PetRoom({ pet }: { pet: PetState }) { const mood = getMood(pet); return <section className="room" aria-label="Pet room"><div className="room__window"><span/><span/><span/></div><div className="room__shelf"><span className="plant"/><span className="tiny-frame"/></div><div className="room__rug"/><div className="room__bed"><div className="bed-pillow"/></div><div className="room__bowl"><div className="bowl-food"/></div><div className="room__toy"><span/></div><div className="room__label"><span className="eyebrow">{pet.isSleeping ? 'Dreaming softly' : moodCopy[mood].hint}</span><h2>{pet.name} is {pet.isSleeping ? 'asleep' : moodCopy[mood].label.toLowerCase()}</h2></div></section> }
